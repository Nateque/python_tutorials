# Tuples are data structure which you cannot change by any methods
# you can only use count, index and slicing methods in tuples

example = ('mango', 'juice', 'old')

print(example[0:2])
print(example.count('mango'))